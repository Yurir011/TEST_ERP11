# 사내 ERP 시스템 구축 프로젝트

> 회사명: BENCH (로고: 원형 크로스헤어 아이콘 + "BENCH" 워드마크, `frontend/src/components/ui/Logo.tsx`)
> 대상: 직원 5명 규모 통신기기 개발·제조업체
> 형태: 사내 서버 기반 인트라넷 웹앱 (외부 비공개)
> 전략: 먼저 뼈대(레이아웃/인증/대시보드)를 완성하고, 이후 기능을 모듈 단위로 하나씩 추가

이 문서는 프로젝트 진행 상황을 추적하는 용도로도 사용한다. **각 단계가 완료되면 체크박스를 `[x]`로 표시**하고, 아래 항목 뒤에 완료 날짜를 `(완료: YYYY-MM-DD)` 형식으로 남긴다.

---

## 코딩 규칙 — 디버그 메시지

**실행되는 모든 프로그램(백엔드 API, 프론트엔드 주요 이벤트, 배치/스크립트 등)에는 디버그 메시지를 반드시 붙인다.**

- 백엔드: 요청 진입/처리결과/에러 지점마다 로거로 출력 (예: `logger.debug(f"[Attendance] 출근 기록 저장 시도: user_id={user_id}")`)
- 프론트엔드: 주요 API 호출 시작/성공/실패 시 `console.debug("[모듈명] ...")` 형태로 출력
- 형식: `[모듈/기능명] 내용` 프리픽스를 통일해서, 로그만 보고도 어느 기능에서 발생했는지 바로 알 수 있게 한다
- 운영 배포 시에는 로그 레벨을 DEBUG → INFO/WARNING 으로 조정할 수 있도록 환경변수로 제어 (`LOG_LEVEL`)
- 목적: 5인 소규모 팀이 서버 로그만 보고도 문제를 빠르게 추적할 수 있도록 하기 위함

---

## 로컬 실행 방법

### 방법 1 — 배치 파일 (권장, 초보자용)
프로젝트 루트의 `start.bat`을 더블클릭하면 백엔드/프론트엔드가 각각 새 창으로 실행되고, 자동으로 브라우저가 열린다.
종료할 때는 `stop.bat`을 실행하거나 열린 두 개의 콘솔 창을 직접 닫으면 된다.

### 방법 2 — 수동 실행
```
# 백엔드 (FastAPI)
cd backend
./venv/Scripts/python.exe -m uvicorn app.main:app --reload --port 8000

# 프론트엔드 (React + Vite)
cd frontend
npm run dev
```
- 프론트엔드: http://localhost:5173
- 백엔드 헬스체크: http://localhost:8000/api/health (또는 프록시 경유 http://localhost:5173/api/health)
- DB 접속 정보는 `backend/.env` 참고 (PostgreSQL, DB명 `erp_db`, 계정 `erp_user`)
- PostgreSQL 슈퍼유저(`postgres`) 비밀번호는 winget 설치 시 설정된 `postgres` — 운영 배포 전 반드시 변경할 것

---

## 1단계 — 요구사항 정리 및 우선순위 분류 [x] (완료: 2026-09-09)
나열한 기능들을 **핵심(Must) / 다음(Should) / 나중(Could)** 3단계로 분류한다.

- **Must (1차 오픈)**: 로그인/권한, 대시보드, 공지사항, 출퇴근기록, 연차관리
- **Should (2차)**: 거래처관리, 프로젝트관리(견적서/거래명세서), 재직증명서 발급
- **Could (3차, 외부 연동 필요·난이도 높음)**: 매입매출(홈택스 연동), 입출금(법인카드 연동, 영수증 OCR)

> 외부 연동 기능(홈택스, 카드사, OCR)은 초기 API 심사·인증 절차가 있어 가장 늦게 배치하는 것이 현실적이다.

---

## 2단계 — 기술 스택 선정 [x] (완료: 2026-09-09)
5인 규모 사내 서버 운영에 맞는 가볍고 유지보수 쉬운 스택을 고른다.

| 영역 | 추천 |
|---|---|
| 프론트엔드 | React + TypeScript + Tailwind CSS (스크린샷과 같은 카드형 디자인 구현 용이) |
| 백엔드 | Node.js (Express/NestJS) 또는 Python (FastAPI) |
| DB | PostgreSQL (관계형, 회계/재고 데이터에 적합) |
| 인증 | JWT + 세션, 추후 사내 AD 연동 고려 |
| 서버 | 사내 미니PC/NAS 또는 저사양 서버 + Docker |
| 배포 | Docker Compose (프론트/백엔드/DB 컨테이너 분리) |

> 초보자 기준이면 Python(FastAPI) + React 조합을 추천 — 러닝커브가 완만하고 자료가 많음.

---

## 3단계 — 시스템 아키텍처 설계 [ ]
- 사내망 전용(외부 접근 차단, VPN 또는 사내 IP 화이트리스트)
- 구조: `React(정적 배포) → Nginx 리버스 프록시 → API 서버 → PostgreSQL`
- 파일 저장(영수증 이미지, 증명서 PDF 등)은 서버 로컬 디스크 + 정기 백업 폴더로 구성
- 향후 원격근무 대비 시 VPN 접속 방식 병행 고려

---

## 4단계 — 데이터베이스 설계 (ERD) [ ]
핵심 테이블부터 설계한다.

- `users` (직원, 권한, 부서, 입사일)
- `attendance` (출퇴근 기록)
- `leaves` (연차 신청/승인 내역)
- `clients` (거래처)
- `projects` / `estimates` (프로젝트, 견적서)
- `transactions` (거래명세서, 매입매출)
- `payments` (입출금, 카드 사용내역)
- `notices` (공지사항)
- `documents` (재직증명서 등 발급 이력)

각 테이블 간 관계(1:N, N:M)를 ERD 툴(draw.io, dbdiagram.io)로 시각화 후 확정.

---

## 5단계 — 디자인 시스템 정의 (첨부 이미지 기반) [x] (완료: 2026-09-09)
첨부 스크린샷 스타일을 기준으로 디자인 토큰을 먼저 확정한다.

- **레이아웃**: 좌측 고정 사이드바(연한 회색 배경) + 우측 메인 컨텐츠(흰색/연한 회색 배경)
- **컬러 팔레트**:
  - 배경: `#F7F7F8` (메인), `#FFFFFF` (카드)
  - 사이드바: `#F2F2F3` 배경, 진한 텍스트
  - 강조색(Primary): 파랑 `#2563EB` (Quick Invoice 버튼 등)
  - 기능별 카드 강조색: 파랑/초록/보라/주황 4색을 기능 카테고리별로 매핑
  - 상태 색: 증가 `#16A34A`(초록), 감소 `#DC2626`(빨강)
- **컴포넌트**: 라운드 카드(radius 12~16px), 얇은 보더, 그림자 최소화, 아이콘+숫자 조합 통계 카드
- **타이포**: 제목(24~28px bold), 본문(14px), 보조텍스트(12~13px 회색)
- Figma 또는 코드 내 Tailwind `theme.extend` 로 위 토큰을 등록해 전 화면에서 재사용

---

## 6단계 — 프로젝트 구조 셋업 [x] (완료: 2026-09-09)
```
/erp-system
  /frontend   (React + Tailwind)
  /backend    (FastAPI or Node)
  /docs       (ERD, 기획 문서)
  docker-compose.yml
```
프론트엔드 폴더는 기능 단위 모듈 구조 채택: `/features/attendance`, `/features/leaves` 등 — 이후 기능 추가 시 폴더만 늘리면 되도록 설계.

---

## 7단계 — 인증 및 권한 시스템 구축 [x] (완료: 2026-09-09, JWT 로그인 + 관리자/일반직원 2단계 권한)
- 로그인 (사번/이메일 + 비밀번호)
- 역할(Role) 구분: 대표/관리자, 일반 직원 (5인 규모이므로 2단계면 충분)
- 관리자만 접근 가능한 메뉴(급여, 매입매출, 설정) 분리
- 비밀번호 초기화, 세션 만료 정책 설정

---

## 8단계 — 공통 레이아웃(뼈대) 구현 [x] (완료: 2026-09-09)
스크린샷과 동일한 틀을 가장 먼저 만든다.

- 사이드바 네비게이션 (로고 + 메뉴 리스트, 현재 메뉴 하이라이트)
- 상단 헤더 없이 페이지 타이틀 + 설명 + 우측 액션 버튼 구조
- 반응형은 우선순위 낮음 (사내 PC 고정 환경이면 데스크톱 우선)
- 라우팅 구조 설정 (React Router)

---

## 9단계 — 대시보드 페이지 구현 [x] (완료: 2026-09-09, 더미 데이터 + 백엔드 헬스체크 연동)
틀이 완성되면 첫 화면인 대시보드를 스크린샷과 유사하게 구현한다.

- Quick Actions 카드 4개 (기능 바로가기)
- 통계 카드 4개 (매출, 거래처 수, 재고, 미결 항목 등 → 이 프로젝트에 맞게 "이번달 근태", "연차 잔여", "진행 프로젝트", "미결 견적" 등으로 재정의)
- 최근 활동(Recent Activity) 리스트
- Quick Stats 사이드 패널
- 초기에는 더미 데이터로 화면만 완성 → 이후 실제 DB 연동

---

## 10단계 — 공지사항 기능 [x] (완료: 2026-09-09, 목록/상세/작성/수정/삭제 + 대시보드 연동)
가장 단순한 기능으로 CRUD 연습 겸 우선 구현.

- 목록/상세/작성/수정/삭제
- 관리자만 작성 가능, 전 직원 열람
- 대시보드 최근 활동과 연계

---

## 11단계 — 출퇴근 기록 기능 [x] (완료: 2026-09-09, 출퇴근 버튼 + 월별 조회(최근 36개월) + 관리자 전체 현황)
- 출근/퇴근 버튼 클릭 방식 (사내 PC에서 직접 입력, 추후 IP 체크 또는 QR 고려)
- 날짜별/직원별 검색 및 월간 캘린더 뷰
- 관리자용 전체 직원 근태 현황 조회

---

## 12단계 — 연차 관리 기능 [x] (완료: 2026-09-09, 신청/승인/반려/취소 + 자동계산 + 승인 시 공지사항 연동)
- 연차 신청 → 승인/반려 워크플로우
- 연차 자동 계산 (입사일 기준 발생 일수)
- 잔여 연차 대시보드 표시
- 캘린더에 팀 연차 현황 표시

---

## 13단계 — 재직/경력증명서 발급 기능 [x] (완료: 2026-09-09, PDF 자동생성 + 재다운로드 + 실제 회사정보 반영)
- 직원이 신청 → 자동으로 회사정보+개인정보 병합한 PDF 생성
- 템플릿(재직증명서, 경력증명서) 미리 설계
- 발급 이력 저장 및 재다운로드 지원

---

## 14단계 — 거래처 관리 기능 [x] (완료: 2026-09-09, 등록/검색/수정/삭제 + 대표자·업종·유선전화·미수금/미지급금)
- 거래처 등록(상호, 사업자번호, 담당자, 연락처, 계좌)
- 거래처별 거래 이력 연동(향후 견적서/매입매출과 연결)
- 검색/필터

---

## 15단계 — 프로젝트 관리 (견적서 · 거래명세서) [x] (완료: 2026-09-09, 세트 채번 연동 + 검색 기능 포함)
- 프로젝트 생성 → 상태 관리(견적/진행/완료)
- 견적서 작성기 (품목, 수량, 단가 자동 계산) → PDF 출력
- 거래명세서 작성 및 발행
- 거래처 관리와 연동하여 자동완성

---

## 16단계 — 매입매출 관리 (세금계산서, 홈택스 연동) [x] (완료: 2026-09-09, 수기 매입/매출 장부 + 부가세 자동계산 리포트. 홈택스 실연동은 팝빌/스마트빌 등 유료 대행 가입 필요 — 보류)
- 우선 수기 입력 기반 매입/매출 장부부터 구현
- 홈택스 전자세금계산서 API(팝빌, 스마트빌 등 중계 서비스 활용 검토 — 홈택스 직접 연동은 개인/기업 인증 절차가 복잡함)
- 부가세 자동 계산 리포트

> 실무 팁: 홈택스 자체 API보다 **팝빌/스마트빌 같은 전자세금계산서 대행 API**를 쓰는 것이 소규모 개발에 훨씬 현실적이다.

---

## 17단계 — 입출금 관리 (법인카드 연동, 영수증 인식) [x] (완료: 2026-09-09, 수기 장부 + 영수증 이미지 첨부 + 카드 CSV 가져오기. OCR 자동인식은 유료 API 키 필요 — 보류)
- 우선 수기 입력 기반 입출금 장부 구현
- 영수증 사진 업로드 → OCR(예: Google Vision API, Naver Clova OCR)로 금액/날짜 자동 인식
- 법인카드 연동은 카드사 API 제공 여부 확인 필요 (없으면 CSV 명세서 업로드 방식으로 대체)

---

## 18단계 — 통합 테스트 및 사용자 피드백 [ ]
- 5명 전 직원 대상 실제 사용 테스트 (1~2주)
- 버그 및 UX 개선사항 수집
- 권한별 시나리오 테스트 (관리자/일반직원)

---

## 19단계 — 사내 서버 배포 및 운영 환경 구축 [ ]
- Docker Compose로 사내 서버에 배포
- 정기 DB 백업 스크립트 (일 1회 자동 백업)
- 사내 도메인/IP 접속 안내 문서 작성
- 사용 매뉴얼(간단한 화면 캡처 가이드) 제작

---

## 20단계 — 유지보수 및 확장 로드맵 수립 [ ]
- 기능별 이슈/요청 관리 (간단한 To-do 시트 또는 GitHub Issues)
- 분기별 기능 추가 계획 (예: 급여관리, 재고관리 고도화)
- 보안 점검 주기 설정 (비밀번호 정책, 접근 로그 점검)
- 데이터 증가에 따른 서버 스펙 재검토 시점 설정 (예: 1년 후)

---

## 우선 추천 진행 순서 요약
```
1~9단계  : 기반 설계 + 공통 틀(레이아웃/대시보드) 완성
10~13단계 : 단순 기능부터 구현 (공지/근태/연차/증명서)
14~15단계 : 거래처 및 프로젝트 관리
16~17단계 : 외부 연동이 필요한 매입매출/입출금 (난이도 최상, 최후순위)
18~20단계 : 테스트, 배포, 운영
```
